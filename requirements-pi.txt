# Raspberry Pi 4 optimized requirements
# Lightweight dependencies for ARM64 edge deployment
# 
# Project: Hareket Tetiklemeli ve Enerji Verimli Gömülü Güvenlik Sistemi
# Target: Raspberry Pi 4 (4GB RAM)

# Core - minimal versions
numpy>=1.24.0,<2.0
opencv-python-headless>=4.8.0
scipy>=1.11.0
Pillow>=10.0.0

# Config
PyYAML>=6.0

# ONNX Runtime for ARM64 - runs ArcFace, MobileFaceNet, RetinaFace
# Pre-built wheels available for ARM64
onnxruntime>=1.16.0

# InsightFace - ArcFace and RetinaFace models
insightface>=0.7.3

# TensorFlow Lite Runtime - optimized for Pi
# Install via: pip install --extra-index-url https://google-coral.github.io/py-repo/ tflite_runtime
# Or: pip install tflite-runtime (if available for your Python version)

# YOLO - Ultralytics (optional, heavier)
# ultralytics>=8.0.0

# Lightweight API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Metrics
scikit-learn>=1.3.0

# Logging
loguru>=0.7.0

# GPIO for Pi (PIR sensor, alerts)
# RPi.GPIO>=0.7.0  # Only on actual Pi
# gpiozero>=2.0.0

# Testing (dev only)
pytest>=7.4.0
